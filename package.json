{
  "name": "mindkey-auth",
  "private": true,
  "scripts": {
    "build": "cd frontend/eeg-auth-app && npm install && npm run build",
    "vercel-build": "cd frontend/eeg-auth-app && npm install && npm run build",
    "start": "cd frontend/eeg-auth-app && npm run preview"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {},
  "devDependencies": {}
}