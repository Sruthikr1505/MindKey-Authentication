services:
  - type: web
    name: mindkey-auth
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app"  # Adjust this to your Python entry point
    envVars:
      - key: PYTHON_VERSION
        value: "3.9"  # Or your Python version
      - key: PORT
        value: 10000
